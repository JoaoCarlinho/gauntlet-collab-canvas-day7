# Railway NIXPACKS configuration for backend Python Flask application
# Completely Docker-free deployment using Railway's native Python infrastructure

[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = [
  "cd backend && pip install --upgrade pip",
  "cd backend && pip install --no-cache-dir --disable-pip-version-check -r requirements.txt"
]

[phases.build]
cmds = [
  "cd backend && python -m py_compile run.py",
  "cd backend && python -m py_compile app/__init__.py"
]

[start]
cmd = "cd backend && python run.py"

[variables]
FLASK_APP = "run.py"
FLASK_ENV = "production"
PYTHONPATH = "/app/backend"
PORT = "5000"
PYTHONUNBUFFERED = "1"
PIP_DISABLE_PIP_VERSION_CHECK = "1"
PIP_NO_CACHE_DIR = "1"
